---
"@as-pect/ctrf-reporter": patch
"@as-pect/cli": patch
"@as-pect/json-reporter": patch
---

Add a dedicated CTRF JSON reporter package and `asp --ctrf` shortcut while documenting that `asp --json` remains the as-pect legacy JSON v1 contract.
